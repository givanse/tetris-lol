
Tetris theme based on LoL

Implementation:
  Pure Javascript, HTML and CSS.
  The objects, tetris shapes, are DIVs that are placed inside another DIV 
  that functions as the board of the game.
  
  The only dependency of the project is QUnit, used for unit testing.
  
Dependencies:
  - QUnit
      test/qunit/qunit.js
      test/qunit/qunit.css
      Those are symbolic links that must point to the qunit files.
      Download from: qunitjs.com
